<h2>Welcome admin</h2>
<hr>

<h3>Generate pairs</h3>
<%= render "form" %>
<hr>
<h3>All pairs</h3>

<% @pairs.all.each do |pair| %>
  <%= pair.day %> :
  <%= pair.matches %>
  <%= link_to "[Delete]", pairs_path(@pairs.id), method: :delete, data: { confirm: "Are you sure?" } %>
<hr>
<% end %>
